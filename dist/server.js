// Hi Michelle!
// how are you?

const run = true


if (run) {
    let distance = 8
    for (var i = 0; i < distance; i++) {
        console.log("running") // print running 8 times
    }
    console.log("Finished running " + distance + " miles") //error - you can fix it by putting let distance at the beggining of the code
}

console.log("Damn, you see this gal? She ran " + distance + " miles")
